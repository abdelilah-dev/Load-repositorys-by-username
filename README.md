# Load repositorys of User by Typing the username
- load 4 repos
- you can load more data if pressed on load more button
- the data is stored in localStorage
- throw an error if the username is empty or not falid
